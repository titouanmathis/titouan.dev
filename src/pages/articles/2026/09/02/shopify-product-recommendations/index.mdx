---
title: Shopify product recommendations
description: Load Shopify product recommendations with @studiometa/ui. Lazy-load the recommendations section, prefetch links and track events with progressive enhancement.
tags: shopify, product-recommendations, lazy-loading, progressive-enhancement, studiometa-ui
---

# Shopify product recommendations

<MetaInfo class="block">02/09/2026 in #shopify #product-recommendations #progressive-enhancement</MetaInfo>

:::info[The series]

**Part 7 of 7** of the series [Building a Shopify storefront with @studiometa/ui](/articles/2026/07/22/building-a-shopify-storefront-with-studiometa-ui/).

:::

Product recommendations are computed per product and returned by their own API, so they are meant to load after the page, not block it. That makes them a good closing example for this series: a section fetched lazily, plus two finishing touches, prefetching and analytics. All of it with [`@studiometa/ui`](https://ui.studiometa.dev) on top of [`@studiometa/js-toolkit`](https://js-toolkit.studiometa.dev).

The demo below tracks an add-to-cart click and fetches a recommendations block after mount:

import demoTwig from './demo.twig?raw';
import demoTs from './demo.ts?raw';

<UiPlayground
  client:visible
  html={demoTwig}
  script={demoTs}
  height="360px"></UiPlayground>

Click "Add to cart" and the analytics line updates; the recommendations arrive a moment after load. [Open it in the playground](https://ui.studiometa.dev/play/#html=eNp1UrFu2zAQ3fMVBw7OUlaB62QwJAWBpwLZ2qWTQZFnmzFFEiTlWC0KFP2YTt2791PyJaVE2rEddNKB9%2FTeu3dXCrkDrpj3FWnZnj5TtQZvGUfa0ztSXwGUJ5CVwj3IgK2nHHVAB0%2BdD3LV0wbDM6IGS2fQGCdiy5lOCxQjSaSJrLpeML1jHhbMwt8%2F8PLz9%2FS2LMZOAjVdCEaPNYBggVFuWmt0FKvIZ8f49tPG2ChITjFhaMy5knxbXX8juItwMgfChFgGs%2BTMBfIOiHVGdHzs8NEH5cyS79eZKs%2BY3NMmj0F559zAV2fYgxAQDAykyXORTI9ZFTGs%2Bmqo7IEv4D7QvYfxm8mKu5vM96CZ6oPkHkbXHmzXKOk3KOYpMpCiIuyAosqsSa1jINBjeI2uLGyS9ciDNIc4N9Pjchv6AVZGB9oYFXfyxXTQsh6Y8gaU3GJZbKb5rzjDfzbwyL72HzVXncCzBRg7iFLveEUKh%2FGPFnXsxEdf5Nj9fS6WcaLZdKJkK0M1m2THw2sG0AuGiYynFuUdKhaOB5VPKhlwuKqIMkxIvSZnyfv2bfKPCQgXOi8%2Ffp0e41sBdM64I%2F1GChEv%2FkzFoaC3N1FiYTolQJsAg6tLpfdnR5%2BOZiiO20tv%2FwDfsDF%2F&script=eNp1VMFu2zgQvfsrBkEASYAlpym6Bzv2Lhq0QItkd9Fm0UNRIDQ1tthQpECO4npdXfZ79r73fkq%2FZIeU7SpNe7HFx3kzw3kzo%2BrGOoIdOFwrT%2BguLSMGDUEHK2drSH7z1JbK1khi8tHnZK2%2BU5TMRurAvRJ%2Fb18ZqdsSx3DjhLx7W9lGrbY%2F9NEq5o6kNZ5gA3PYKFPazWy0KQ4sxo7fnz%2FDrhtcFsIIvSUl%2FdBsgAbCCKBpl1r5agop3vNrxtCIrbaizGC%2BiAYAfQrartlTaWVbs12xRnqhMXw%2B374q0%2BToOGfDJJtFplpByscskAvCT3RpDYWazeH2dBcDdnC6e%2F32j98LT06ZNeeYHjLoboOXbjzqjnVwKG3NQUss2cd7vt4BKdI4heQdFxyeozAKkzFUwpQR3jCcLw9w45Rk9Pw8%2BB2yr1BQhQ5uLD2g6x5nOWnA%2F%2BXse%2F4lOlErCdftekiXPZzXEd6znzyKfmmJrIEbfBBbRjQnHER%2BGiN%2FmI1WrZGkmHSsiQhHfy3cXdukWRSvr5oirEMbDKoX5Slq0aRpE6W%2BvdBqcSGgcrian0waZ8tWkp%2Bc7pqiT6g7AamF9%2FOTpbbyDpr8KSytK7lozrZRksreo5su17lsnWNxJ89OFsFBfGYHX%2F6Dr%2F%2F8G4D4mO5iIhYXE457m%2FX5fLTKpEnfPQ6pdYbzavUh7tqpEsJPLq32%2BTmsBScRQsQXsr9WL7hpum%2FtIvRLJFkdx6dYhWOx5EPaIxxseMWWwm%2BNhFSZpuV5UEbRcRh6t63TbGZwA3%2B9uUpp26BdQTSH%2BXwOSd%2FKCfy6B6f9f8G8MY%2BCjEIVodLxpWFK%2BKpouNOMqLFghfw7RVWaTL4T96hLkmX76RQboSgm8yevEOUxTR16q%2B8xZu2RblSNtqUDPIZnZ2fZfkL3VQ70N%2Bh5ozH%2FJw015n6tULDefsqf3J1xlvPw%2FoS7Ncz3pKJaJ7zOuui%2F%2B6bjUYoHdZ3F2X60U9PBpmSbx%2FfD5ZnN%2FgcWQOQh&theme=light) to edit it.

<TableOfContent open />

## Lazy-loading the recommendations section

The [Product Recommendations API](https://shopify.dev/docs/api/ajax/reference/product-recommendations) returns recommendations for a product at `GET /recommendations/products`. Pass a `section_id` and it renders one of your theme sections with the recommendations filled in, so the response is ready-to-inject HTML. The `recommendations` object is empty on the initial page render, which is exactly why this is a client-side fetch.

[`LazyInclude`](https://ui.studiometa.dev/components/LazyInclude/) is built for this. Point its `src` at the endpoint and it fetches on mount, injects the HTML, and manages a loading and an error state through refs:

```liquid
<div
  data-component="LazyInclude"
  data-option-src="{{ routes.product_recommendations_url }}?section_id=product-recommendations&product_id={{ product.id }}&limit=4&intent=related">
  <span data-ref="loading">Loading recommendations…</span>
  <span data-ref="error" hidden>Could not load recommendations.</span>
</div>
```

```js twoslash
// @noErrors
import { registerComponent } from '@studiometa/js-toolkit';
import { LazyInclude } from '@studiometa/ui';

registerComponent(LazyInclude);
```

`LazyInclude` hides the `loading` ref and injects the response into its own element when the content arrives, and reveals the `error` ref if the request fails. Use `intent=related` for "You may also like" and `intent=complementary` for "Pairs well with"; the API caps results at 10.

To defer the fetch until the section is near the viewport (as Dawn does with an `IntersectionObserver`), pair it with [`Sentinel`](https://ui.studiometa.dev/components/Sentinel/), which reports when an element enters the viewport through a parent's `onSentinelIntersected(entries)` handler. `LazyInclude` on mount is the simplest option and is fine when the block sits below the fold on a normal-length page.

## How the demo simulates it

The playground has no store, so the demo mocks `GET /recommendations/products` to return a rendered product list, and stubs `window.Shopify.analytics` so the tracking has somewhere to publish:

```js twoslash
// @noErrors
window.fetch = async (input, init) => {
  const url = new URL(
    typeof input === 'string' ? input : input instanceof URL ? input.href : input.url,
    'http://localhost',
  );
  if (url.pathname.endsWith('/recommendations/products')) {
    return new Response(recommendationsMarkup(), { headers: { 'content-type': 'text/html' } });
  }
  return realFetch(input, init);
};
```

**Swap the mock for your store:** delete the `window.fetch` block and the `window.Shopify` stub. The `src` already points at the real recommendations route, and `window.Shopify.analytics` exists on a live storefront. Use locale-aware URLs (`{{ routes.product_recommendations_url }}` in Liquid, `window.Shopify.routes.root` in JavaScript).

## Tracking events

[`TrackShopify`](https://ui.studiometa.dev/components/Track/) publishes analytics events declaratively through `window.Shopify.analytics.publish`. Add a `data-track:<event>` attribute with a JSON payload, and the event fires on that DOM event:

```html
<button
  data-component="TrackShopify"
  data-track:click='{"event": "add_to_cart", "product": "canvas-cap"}'>
  Add to cart
</button>
```

Its sibling [`Track`](https://ui.studiometa.dev/components/Track/) publishes to `window.dataLayer` for Google Tag Manager or Google Analytics 4 (GA4). `TrackContext` lets a parent supply payload fields inherited by every tracked element inside it, for stamping a list name or position onto product-card events. The event name is the payload's `event` key; you can also track `mounted` (fires on mount, for impressions) and a viewport-based `view` event.

## Prefetching links

The last touch cuts the delay when a customer follows a product link. [`PrefetchWhenVisible`](https://ui.studiometa.dev/components/Prefetch/) adds a `<link rel="prefetch">` for a link once it scrolls into view, so the destination is warm by the time the customer clicks:

```html
<a href="{{ product.url }}" data-component="PrefetchWhenVisible">{{ product.title }}</a>
```

Its sibling `PrefetchWhenOver` prefetches on `mouseenter` instead. Both only prefetch same-origin URLs, skip the current page, and de-duplicate across the page, so they are safe to add to product links.

One caveat: components only mount on elements that exist when you register them. Product links rendered by your Liquid templates are enhanced normally. Links injected later (for instance, the recommendations `LazyInclude` just loaded) are not mounted automatically. To enhance injected markup, re-run registration once the content is in, using the `content` event `LazyInclude` emits, or mount the subtree with a js-toolkit app. Keep `PrefetchWhenVisible` and `TrackShopify` on server-rendered elements unless you add that step.

## The end of the series

That completes the storefront: [AJAX navigation and section rendering](/articles/2026/07/29/shopify-section-rendering-api-example/), [collection filtering](/articles/2026/08/05/shopify-ajax-collection-filtering/), a [variant selector](/articles/2026/08/12/shopify-variant-selector/), an [AJAX cart drawer](/articles/2026/08/19/shopify-ajax-cart-drawer/), [predictive search](/articles/2026/08/26/shopify-predictive-search/), and now recommendations. Every piece enhances the HTML your theme already renders. The through-line, and the setup, live in the [pillar](/articles/2026/07/22/building-a-shopify-storefront-with-studiometa-ui/).
